import 'package:flutter/material.dart';

class CreateExerciseWidget extends StatelessWidget {
  const CreateExerciseWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
